# cinfra
Deploy Lambdas to AWS. 2 services, each with a single Lambda. The first takes an argument of type string, and returns the number of characters in it. The second takes a JSON string and returns an indication of whether the JSON was valid. The Lambda should be accessible via a URL.
